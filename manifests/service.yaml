apiVersion: v1
kind: Service
metadata:
  name: slm-inference
  labels:
    app: slm-inference
spec:
  selector:
    app: slm-inference
  ports:
    - name: http
      port: 8080
      targetPort: 8080
